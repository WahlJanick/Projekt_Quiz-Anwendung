<Page x:Class="WPF_Quiz_Anwendung.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPF_Quiz_Anwendung"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage"
      Background="#F3C8FF">

    <Page.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D88BEF"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C06ADF"/>
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.99" ScaleY="0.99"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Viewbox Stretch="Uniform" StretchDirection="Both">
        <Grid Width="800" Height="450" ShowGridLines="False" Background="#F3C8FF">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Image Source="/Images/QuizzzardLogo.png" Grid.Column="0" Grid.ColumnSpan="3"></Image>
            <Image Source="/Images/Group 5.png" Grid.Column="2" Grid.ColumnSpan="2"></Image>
            <Button x:Name="Standard_Quiz" Grid.Column="0" Grid.Row="1" Margin="30 80 30 80" Background="#C772DF" BorderBrush="Purple" Content="Standart Quiz" FontFamily="Grandstander" Click="OpenDefaultQuiz" Style="{StaticResource RoundedButtonStyle}"></Button>
            <Button x:Name="Load_Quiz" Grid.Column="1" Grid.Row="1" Margin="30 80 30 80" Background="#C772DF" BorderBrush="Purple" Content="Quiz Laden" FontFamily="Grandstander" Click="LoadQuiz" Style="{StaticResource RoundedButtonStyle}"></Button>
            <Button x:Name="Create_Quiz" Grid.Column="2" Grid.Row="1" Margin="30 80 30 80" Background="#C772DF" BorderBrush="Purple" Content="Quiz erstellen" FontFamily="Grandstander" Click="Navigate_CreateQuizPage" Style="{StaticResource RoundedButtonStyle}"></Button>
            <Button x:Name="Settings" Grid.Column="3" Grid.Row="1" Margin="30 80 30 80" Background="#C772DF" BorderBrush="Purple" Content="Einstellungen" FontFamily="Grandstander" Click="Navigate_SettingsPage" Style="{StaticResource RoundedButtonStyle}"></Button>
        </Grid>
    </Viewbox>
</Page>